/*!
 * vue-typed 2.1.3
 * Sets of ECMAScript / Typescript decorators that helps you write Vue component easily.
 * http://vue-typed.github.io/vue-typed/
 *   
 * Copyright 2016-2017, Budi Adiono
 * Released under the MIT license.
 */
(function webpackUniversalModuleDefinition(root, factory) {
	if(typeof exports === 'object' && typeof module === 'object')
		module.exports = factory(require("vue"));
	else if(typeof define === 'function' && define.amd)
		define(["vue"], factory);
	else {
		var a = typeof exports === 'object' ? factory(require("vue")) : factory(root["vue"]);
		for(var i in a) (typeof exports === 'object' ? exports : root)[i] = a[i];
	}
})(this, function(__WEBPACK_EXTERNAL_MODULE_0__) {
return /******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, {
/******/ 				configurable: false,
/******/ 				enumerable: true,
/******/ 				get: getter
/******/ 			});
/******/ 		}
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 1);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, exports) {

module.exports = __WEBPACK_EXTERNAL_MODULE_0__;

/***/ }),
/* 1 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
Object.defineProperty(__webpack_exports__, "__esModule", { value: true });

// CONCATENATED MODULE: ./src/lib/utils.ts
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_vue__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_vue___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_vue__);
/**
 * Internal utilities.
 */

var vueInternalPropNames = Object.getOwnPropertyNames(new __WEBPACK_IMPORTED_MODULE_0_vue__());
var vueInternalHooks = [
    'data',
    'props',
    'watch',
    'beforeCreate',
    'created',
    'beforeMount',
    'mounted',
    'beforeUpdate',
    'updated',
    'activated',
    'deactivated',
    'beforeDestroy',
    'destroyed',
    'render'
];
/** @internal */
var PROP_KEY = '$_vt_props';
/** @internal */
function BuildOptions(Component, options) {
    // evaluate component name
    if (!options) {
        options = {};
    }
    options.name = options.name || Component.name;
    // class prototype.
    var proto = Component.prototype;
    // avoid parent component initialization while building component
    if (Object.getPrototypeOf(proto) instanceof __WEBPACK_IMPORTED_MODULE_0_vue__)
        Object.setPrototypeOf(proto.constructor, function () { });
    var constructor = new proto.constructor();
    //
    // Extract props / build options.props
    //
    var propAttrs = proto[PROP_KEY];
    var propNames = undefined;
    delete proto[PROP_KEY];
    if (propAttrs) {
        var props = {};
        propNames = Object.getOwnPropertyNames(propAttrs);
        for (var i = 0; i < propNames.length; i++) {
            var prop = propNames[i];
            var propVal = undefined;
            var descriptor = Object.getOwnPropertyDescriptor(propAttrs, prop);
            var constructorDefault = constructor[prop];
            if (typeof (descriptor.value) === 'object') {
                // prop options defined
                propVal = descriptor.value;
                // try to assign default value
                if (!propVal.default)
                    propVal.default = constructorDefault;
            }
            else if (constructorDefault) {
                // create prop object with default value from constructor
                propVal = {
                    default: constructorDefault
                };
            }
            // just define empty prop
            if (typeof (propVal) === 'undefined') {
                propVal = true;
            }
            props[prop] = propVal;
        }
        if (!options.props) {
            options.props = {};
        }
        for (var p in props) {
            options.props[p] = props[p];
        }
    }
    //
    // build internal hooks, methods and computed properties
    //
    Object.getOwnPropertyNames(proto).forEach(function (key) {
        // skip constructor     
        if (key === 'constructor') {
            return;
        }
        // internal hooks
        if (vueInternalHooks.indexOf(key) > -1) {
            options[key] = proto[key];
            return;
        }
        var descriptor = Object.getOwnPropertyDescriptor(proto, key);
        if (typeof descriptor.value === 'function') {
            // methods
            (options.methods || (options.methods = {}))[key] = descriptor.value;
        }
        else if (descriptor.get || descriptor.set) {
            // computed properties
            (options.computed || (options.computed = {}))[key] = {
                get: descriptor.get,
                set: descriptor.set
            };
        }
    });
    //
    // Build options.data
    //
    var dataNames = [];
    var restrictedNames = vueInternalPropNames;
    if (propNames)
        restrictedNames = restrictedNames.concat(propNames);
    // gather data from constructor
    Object.getOwnPropertyNames(constructor).forEach(function (key) {
        if (restrictedNames.indexOf(key) === -1)
            dataNames.push(key);
    });
    if (dataNames.length > 0) {
        // evaluate parent data
        var parentData_1 = undefined;
        var parentDataType = typeof options.data;
        if (parentDataType === 'function') {
            parentData_1 = options.data();
        }
        else if (parentDataType === 'object') {
            parentData_1 = options.data;
        }
        options.data = function () {
            // define new data object
            var data_obj = parentData_1 || {};
            // set data default values initialized from constructor
            dataNames.forEach(function (prop) {
                var descriptor = Object.getOwnPropertyDescriptor(constructor, prop);
                if (!descriptor.get && !descriptor.set && typeof descriptor.value !== 'function') {
                    data_obj[prop] = constructor[prop];
                }
            });
            return data_obj;
        };
    }
    return options;
}

// CONCATENATED MODULE: ./src/lib/component.ts
/* harmony import */ var component___WEBPACK_IMPORTED_MODULE_0_vue__ = __webpack_require__(0);
/* harmony import */ var component___WEBPACK_IMPORTED_MODULE_0_vue___default = __webpack_require__.n(component___WEBPACK_IMPORTED_MODULE_0_vue__);


/**
 * Build Vue component.
 *
 * @export
 * @param {ComponentOptions<Vue>} [options]
 * @returns {ClassDecorator}
 */
function component_Component(options) {
    var factory = function (Component, options) {
        // Build vue component
        var superProto = Object.getPrototypeOf(Component.prototype);
        var Super = superProto instanceof component___WEBPACK_IMPORTED_MODULE_0_vue__
            ? superProto.constructor
            : component___WEBPACK_IMPORTED_MODULE_0_vue__;
        return Super['extend'](BuildOptions(Component, options));
    };
    if (options instanceof Function) {
        return factory(options);
    }
    return function (Component) {
        return factory(Component, options);
    };
}

// CONCATENATED MODULE: ./src/lib/options.ts

/**
 * Build Vue simple raw options object.
 * Inheritance is not supported, unless virtual inheritance.
 *
 * @export
 * @param {ComponentOptions<Vue>} [options]
 * @returns {ClassDecorator}
 */
function Options(options) {
    return function (Component) {
        function chainUp(component) {
            var Super = Object.getPrototypeOf(component);
            if (Super instanceof Object && Super.prototype) {
                var keys = Object.getOwnPropertyNames(Super.prototype);
                for (var i = 0; i < keys.length; i++) {
                    var key = keys[i];
                    var descriptor = Object.getOwnPropertyDescriptor(Super.prototype, key);
                    if (!Component.prototype.hasOwnProperty(key))
                        Object.defineProperty(Component.prototype, key, descriptor);
                }
                chainUp(Super);
            }
        }
        // normalize object
        chainUp(Component);
        return BuildOptions(Component, options);
    };
}

// CONCATENATED MODULE: ./src/lib/prop.ts

/**
 * Treat a class member as a Vue prop.
 *
 * @export
 * @param {PropOptions} [options]
 * @returns {PropertyDecorator}
 */
function Prop(options) {
    return function (target, key) {
        if (!target[PROP_KEY]) {
            target[PROP_KEY] = {};
        }
        if (!target[PROP_KEY][key]) {
            if (options) {
                target[PROP_KEY][key] = options;
            }
            else {
                target[PROP_KEY][key] = true;
            }
        }
    };
}

// CONCATENATED MODULE: ./src/lib/watch.ts
/**
 * Treat a function as a Vue watcher.
 *
 * @export
 * @param {string} property
 * @param {boolean} [deep]
 * @returns {MethodDecorator}
 */
function Watch(property, deep) {
    return function (target, key) {
        if (typeof target['watch'] !== 'object') {
            target['watch'] = {};
        }
        if (!target['watch'][property]) {
            var watcher;
            if (deep !== undefined) {
                watcher = {
                    handler: target[key],
                    deep: deep
                };
            }
            else {
                watcher = target[key];
            }
            target['watch'][property] = watcher;
        }
    };
}

// CONCATENATED MODULE: ./src/lib/mixins.ts
/* harmony import */ var mixins___WEBPACK_IMPORTED_MODULE_0_vue__ = __webpack_require__(0);
/* harmony import */ var mixins___WEBPACK_IMPORTED_MODULE_0_vue___default = __webpack_require__.n(mixins___WEBPACK_IMPORTED_MODULE_0_vue__);

/**
 * Single mixin.
 *
 * @export
 * @template T
 * @param {{ new () : T; }} component
 * @returns {VirtualClass<T>}
 */
function Mixin(component) {
    return mixins___WEBPACK_IMPORTED_MODULE_0_vue__["extend"]({
        mixins: [component]
    });
}
/**
 * Multiple mixins.
 *
 * @export
 * @template T
 * @param {...{ new () : any; }[]} components
 * @returns {VirtualClass<T>}
 */
function Mixins() {
    var components = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        components[_i] = arguments[_i];
    }
    return mixins___WEBPACK_IMPORTED_MODULE_0_vue__["extend"]({
        mixins: components
    });
}

// CONCATENATED MODULE: ./src/lib/mixins-global.ts
/* harmony import */ var mixins_global___WEBPACK_IMPORTED_MODULE_0_vue__ = __webpack_require__(0);
/* harmony import */ var mixins_global___WEBPACK_IMPORTED_MODULE_0_vue___default = __webpack_require__.n(mixins_global___WEBPACK_IMPORTED_MODULE_0_vue__);


/**
 * Build Vue global mixin.
 *
 * @export
 * @param {ComponentOptions<Vue>} [options]
 * @returns {ClassDecorator}
 */
function GlobalMixin(options) {
    var factory = function (Component, options) {
        mixins_global___WEBPACK_IMPORTED_MODULE_0_vue__["mixin"](BuildOptions(Component, options));
    };
    return function (Component) {
        return factory(Component, options);
    };
}

// CONCATENATED MODULE: ./src/lib/common.ts
/**
 * Common helpers.
 */
/**
 * Resulting an empty constructor with Vue object declarations from "T".
 * Useful to create a fake inheritance.
 *
 * @export
 * @template T
 * @returns {VirtualClass<T>}
 */
function Virtual() {
    return function () { };
}

// CONCATENATED MODULE: ./src/index.ts
/* concated harmony reexport */__webpack_require__.d(__webpack_exports__, "Component", function() { return component_Component; });
/* concated harmony reexport */__webpack_require__.d(__webpack_exports__, "Options", function() { return Options; });
/* concated harmony reexport */__webpack_require__.d(__webpack_exports__, "Prop", function() { return Prop; });
/* concated harmony reexport */__webpack_require__.d(__webpack_exports__, "Watch", function() { return Watch; });
/* concated harmony reexport */__webpack_require__.d(__webpack_exports__, "Mixin", function() { return Mixin; });
/* concated harmony reexport */__webpack_require__.d(__webpack_exports__, "Mixins", function() { return Mixins; });
/* concated harmony reexport */__webpack_require__.d(__webpack_exports__, "GlobalMixin", function() { return GlobalMixin; });
/* concated harmony reexport */__webpack_require__.d(__webpack_exports__, "Virtual", function() { return Virtual; });
/**
 * Main module
 */









/***/ })
/******/ ]);
});